name: bootstrap-concluido

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Bootstrap concluído; projeto já publicado."
